// ARRAYS:
var a = [1, 2, 3, 4, 5];
// spread operator copy values from a and then stores it in b
var b = [...a];

// OBJECTS:
var obj = { name: 'Mustafa Ahmed' };
var copyObj = { ...obj };

// IMPORTANT ====> if we make any changes in copy array or object it will not affect the original array/object
